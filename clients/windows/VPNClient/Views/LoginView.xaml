<UserControl x:Class="VPNClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="380">

    <Grid>
        <StackPanel>
            <!-- Header -->
            <TextBlock Text="Sign In" FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
            <TextBlock Text="Enter your credentials to connect"
                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,0,0,24"/>

            <!-- Username -->
            <TextBlock Text="Username" FontSize="12" FontWeight="Medium"
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
            <TextBox x:Name="UsernameTextBox"
                     Style="{StaticResource ModernTextBoxStyle}"
                     Margin="0,0,0,16"/>

            <!-- Password -->
            <TextBlock Text="Password" FontSize="12" FontWeight="Medium"
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
            <PasswordBox x:Name="PasswordBox"
                         Style="{StaticResource ModernPasswordBoxStyle}"
                         Margin="0,0,0,8"/>

            <!-- Remember Me -->
            <CheckBox x:Name="RememberMeCheckBox" Content="Remember me"
                      Margin="0,0,0,24" Foreground="{StaticResource TextPrimaryBrush}"/>

            <!-- Error Message -->
            <TextBlock x:Name="ErrorMessage" Text="" TextWrapping="Wrap"
                       Foreground="{StaticResource ErrorBrush}" FontSize="12"
                       Margin="0,0,0,16" Visibility="Collapsed"/>

            <!-- Login Button -->
            <Button x:Name="LoginButton" Style="{StaticResource ModernButtonStyle}"
                    Content="Sign In" Click="LoginButton_Click"
                    HorizontalAlignment="Stretch"/>

            <!-- Loading Indicator -->
            <StackPanel x:Name="LoadingPanel" Orientation="Horizontal"
                        HorizontalAlignment="Center" Margin="0,16,0,0"
                        Visibility="Collapsed">
                <ProgressBar IsIndeterminate="True" Width="100" Height="3"
                             Foreground="{StaticResource PrimaryBrush}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
